
const canvas=document.getElementById('stars');
if(canvas){const c=canvas.getContext('2d');let W,H,pts=[];function size(){W=canvas.width=innerWidth*devicePixelRatio;H=canvas.height=innerHeight*devicePixelRatio;c.scale(devicePixelRatio,devicePixelRatio);W=innerWidth;H=innerHeight;pts=Array.from({length:100},()=>({x:Math.random()*W,y:Math.random()*H,z:Math.random()*1.5+.4}))}size();addEventListener('resize',size);function draw(){c.clearRect(0,0,W,H);for(const p of pts){p.y-=.12*p.z;if(p.y<0)p.y=H;c.beginPath();c.arc(p.x,p.y,p.z,0,7);c.fillStyle='rgba(160,220,255,.35)';c.fill()}requestAnimationFrame(draw)}draw()}
const clock=document.getElementById('clock');if(clock){function t(){clock.textContent=new Date().toLocaleString('en-IN',{weekday:'long',day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',second:'2-digit'})}t();setInterval(t,1000)}
const scene=document.querySelector('.campus3d'),wrap=document.querySelector('.scene');if(scene&&wrap){wrap.addEventListener('mousemove',e=>{let r=wrap.getBoundingClientRect(),x=e.clientX/r.width-.5,y=e.clientY/r.height-.5;scene.style.transform=`rotateX(${8-y*12}deg) rotateY(${-13+x*24}deg)`});wrap.addEventListener('mouseleave',()=>scene.style.transform='rotateX(8deg) rotateY(-13deg)')}
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));
document.querySelectorAll('[data-count]').forEach(el=>{io.observe(el);let done=false;const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting&&!done){done=true;let n=+el.dataset.count,x=0;let q=setInterval(()=>{x+=Math.ceil(n/55);if(x>=n){x=n;clearInterval(q)}el.textContent=x+(n>=200?'+':'')},22)}}),{threshold:.6});o.observe(el)});
